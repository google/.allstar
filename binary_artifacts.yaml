optConfig:
  optOutStrategy: true
  optOutArchivedRepos: true
  # Add a repo-name if you wish to remove a repo from allstar checks
  optOutRepos:
  - capsicum-linux
action: issue
ignoreFiles:
- gradle-wrapper.jar

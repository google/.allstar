optOutStrategy: true  # enable the opt-out feature
optOutArchivedRepos: true  # exclude archived repositories from checks
optOutRepos:  # list of repository names to exclude from checks
  - capsicum-linux
action: issue  # specify that issues should be created for violations
ignoreFiles:  # list of files to ignore during checks
  - gradle-wrapper.jar

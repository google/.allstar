optConfig:
  optOutStrategy: true
  # Uncomment and change the repo-name if you wish to remove a repo from allstar checks
  # optOutRepos:
  # - repo-name-one
  # - repo-name-two
issueFooter: Allstar has been installed on all Google managed GitHub orgs. Policies are gradually being rolled out and enforced by the GOSST and OSPO teams. Learn more at http://go/allstar-github
